# ====================================================================
# 1. ИГНОРИРУЕМ СЫРЫЕ И ОБРАБОТАННЫЕ ДАТАСЕТЫ (КУЧУ ФОТОК И РАЗМЕТКУ)
# ====================================================================
/hold/
/test/
/tier3/
/train/
/yolo-damage/data/

# Игнорируем временную папку runs в корне (если создалась при тестах)
/runs/

# ====================================================================
# 2. ИГНОРИРУЕМ КОНКРЕТНЫЕ ПРЕРВАННЫЕ ЗАПУСКИ В YOLO-DAMAGE/RUNS
# ====================================================================
# Эти папки останутся у тебя на компьютере, но Git их проигнорирует:
/yolo-damage/runs/damage_yolo_run1/
/yolo-damage/runs/damage_yolo_run1-2/
/yolo-damage/runs/damage_yolo11s_100ep/
/yolo-damage/runs/damage_yolo11s_100ep-2/
/yolo-damage/runs/damage_yolo11s_100ep-4/

# ====================================================================
# 3. ИГНОРИРУЕМ БАЗОВЫЕ ПРЕДОБУЧЕННЫЕ МОДЕЛИ В КОРНЕ
# ====================================================================
# Маска /*.pt проигнорирует только файлы .pt в корневой папке,
# но НЕ тронет файлы весов внутри наших успешных запусков:
/*.pt

# ====================================================================
# 4. СИСТЕМНЫЕ ФАЙЛЫ И КЭШ
# ====================================================================
__pycache__/
*.pyc
*.pyo
*.pyd
.ipynb_checkpoints/
.virtual_documents/
.DS_Store
Thumbs.db